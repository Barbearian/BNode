using UnityEngine;

public class HotFixDemoScene : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello Bear, You made it LOL");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
